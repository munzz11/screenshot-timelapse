version: '3.8'

services:
  timelapse-generator:
    build: .
    container_name: timelapse-generator
    volumes:
      - ${SCREENSHOTS_PATH:-./screenshots}:/data:ro
    environment:
      - SCREENSHOTS_PATH=/data
    restart: unless-stopped
    # Use the startup script from the Dockerfile 